/**
 * Bump when calibrated generation or grader acceptance semantics change.
 * Prepared pool items with a mismatched version are discarded on load.
 */
export const PUZZLE_GENERATOR_VERSION = 1 as const
